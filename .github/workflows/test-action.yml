name: Test Action

on:
  push:
    branches: [ main ]  # triggers on push to main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 24f3005132@ds.study.iitm.ac.in
        run: echo "Hello, world!"
